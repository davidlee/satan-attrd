---
id: IP-001
slug: lint_cleanup_make_tree_green_under_pol_001_strict_clippy
name: "Implementation Plan - Lint cleanup: make tree GREEN under POL-001 strict clippy"
created: "2026-05-31"
updated: "2026-05-31"
status: draft
kind: plan
aliases: []
---

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-001
delta: DE-001
revision_links:
  aligns_with:
    - DR-001
specs:
  primary: []
  collaborators: []
requirements:
  targets: []
  dependencies: []
phases:
  - id: IP-001-P01
```

```yaml supekku:verification.coverage@v1
schema: supekku.verification.coverage
version: 1
subject: IP-001
entries:
  - artefact: just-lint
    kind: VA
    requirement: POL-001
    status: planned
    notes: "`just lint` exits 0 with zero warnings. `cargo fmt --all --check` passes."
```

## 1. Summary

- **Delta**: DE-001 – Lint cleanup: make tree GREEN under POL-001 strict clippy
- **Desired Outcome**: `just lint` silent, `cargo fmt --all --check` passes

## 2. Context & Constraints

- **Current Behaviour**: ~40 clippy violations under POL-001 strict config
- **Target Behaviour**: Zero warnings, zero format diffs
- **Dependencies**: None
- **Constraints**: No config changes, no business logic changes, `#[expect]` not `#[allow]`

## 3. Gate Check

- [x] Policy POL-001 linked
- [x] DR-001 approved (no architectural changes)
- [x] Test strategy: lint gate only (DB tests out of scope)
- [x] Workspace/config: lint config already installed

## 4. Phase Overview

| Phase                              | Objective                          | Entrance Criteria     | Exit Criteria                     | Phase Sheet          |
| ---------------------------------- | ---------------------------------- | --------------------- | --------------------------------- | -------------------- |
| Phase 1 - Fix all lint violations  | Resolve every clippy warning       | DR approved           | `just lint` silent, fmt clean     | `phases/phase-01.md` |

Single-phase: all violations are mechanical and independent. Fix file-by-file, re-running `just lint` after each.

## 5. Phase Detail Snapshot

- **Design Revision**: `DR-001.md` (approved)
- **Active Phase Sheet**: `phases/phase-01.md`
- **Work items reference**: `LINT-CLEANUP-HANDOFF.md` items A–J
- **Footgun reference**: `.spec-driver/memory/mem.pattern.project.rust-lint-footguns.md`

## 6. Testing & Verification Plan

- **Gate**: `just lint` = `cargo clippy --all-targets --all-features -- -D warnings` — must be silent
- **Format**: `cargo fmt --all` then `cargo fmt --all --check` — must pass
- **DB tests**: Out of scope (`just check` requires Postgres)

## 7. Risks & Mitigations

| Risk                                   | Mitigation                              | Owner |
| -------------------------------------- | --------------------------------------- | ----- |
| Second-wave lints in tests/*.rs hidden | Run `just lint` to convergence          | Agent |
| Line numbers drift during edits        | Re-run clippy after each file; relocate | Agent |
| `#[expect]` unfulfilled errors         | Trust clippy output; remove missing lints from list | Agent |

## 8. Open Questions & Decisions

None.

## 9. Progress Tracking

- [ ] Phase 1 complete
- [ ] Verification gates passed
- [ ] Lint notes disclosure per POL-001 §Verification
